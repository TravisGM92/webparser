<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <% x = 0 %>
    <% @all_supplements.each do |supplement| %>
      <div class="supplement-<%=x%>">

        <form class="supplement" action="/supplements" method="post">
          <label for="id" class="hidden"></label>
          <input type="text" name="id" class="hidden" value="<%=supplement.id%>">
          <input type="submit" name="" class="supplement-button" value="<%=supplement.title%>">
          <p>Good for...</p><%supplement.categories.each_with_index do |category, index| %>
            <% if supplement.categories.length == 1 %>
              <%= category.keyword.capitalize %>
            <% else %>
              <% if index == 0 %>
                <%= category.keyword.capitalize %>,
              <% elsif category != supplement.categories[-1] %>
                <%= category.keyword %>,
              <% else %>
                <%= category.keyword %>
              <% end %>
            <% end %>
          <% end %>
            <br><br>
            <hr>
          </form>
        </div>
      <%x += 1 %>
    <% end %>

  </body>
</html>
